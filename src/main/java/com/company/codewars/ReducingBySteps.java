package com.company.codewars;

import java.util.Arrays;
import java.util.Iterator;
import java.util.NoSuchElementException;
import java.util.function.BiFunction;
import java.util.function.Function;
import java.util.function.LongUnaryOperator;
import java.util.stream.LongStream;

public class ReducingBySteps {
   /* public static long gcdi(long x, long y) {
        while (x != y) // while the two numbers are not equal...
        {

            if (x > y) x -= y; // if x is larger than y, subtract y from x
            else y -= x; // if y is larger than x, subtract x from y
        }

        return x;
    }
    public static long lcmu(long a, long b) {
        return (a * b) / gcdi(a, b);
    }
    public static long som(long a, long b) {
        return a +b;
    }
    public static long maxi(long a, long b) {
        return Math.max(a, b);
    }
    public static long mini(long a, long b) {
       return  Math.min(a,b);
    }
    public static long[] operArray(BiFunction<Long, Long, Long> function, long[] arr, long init) {
        arr.
    }*/
}
